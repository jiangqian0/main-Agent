# AliCloud Agent Hub - 设计文档

> 文档版本：v1.0
> 创建日期：2026-05-17
> 最后更新：2026-05-17

---

## 1. 产品定位

### 1.1 产品概述

AliCloud Agent Hub 是一款面向企业的 AI Agent 平台，深度集成阿里云生态。用户可以通过自然语言对话完成云资源管理、一键部署、批量运维、安全审计、成本分析等企业级运维场景。

### 1.2 核心能力矩阵

| 能力维度 | 说明 |
|---------|------|
| **对话交互** | 自然语言驱动的智能对话，理解用户意图并调度工具 |
| **代码生成** | 编写、编辑、执行代码及部署脚本 |
| **云资源管理** | 查询/操作 ECS、ACK、RDS、VPC 等阿里云资源 |
| **一键部署** | 模板化部署 ACK、ECS 等资源，降低运维门槛 |
| **批量运维** | 批量执行脚本、批量重启/扩缩容 |
| **安全审计** | RAM 权限扫描、合规检查 |
| **成本分析** | 云资源账单分析、优化建议 |
| **知识管理** | 企业文档 + RAG 知识库，增强回答准确性 |

### 1.3 目标用户

| 用户角色 | 典型场景 |
|---------|---------|
| **开发者** | 写代码、查日志、调用阿里云 API |
| **运维 / SRE** | 管理 ECS/ACK、批量操作、监控告警 |
| **架构师 / 管理层** | 查看成本趋势、资源使用、做决策 |
| **安全工程师** | 权限扫描、合规审计 |

---

## 2. 系统架构

### 2.1 Agent 架构（方案 B：主 Agent + 独立 Sub-Agent）

```
┌─────────────────────────────────────────────────────┐
│                    用户                              │
└─────────────────────┬───────────────────────────────┘
                      │
              ┌───────▼────────┐
              │  主 Agent       │
              │  (统一入口)     │
              └───────┬────────┘
                      │ 调度
        ┌─────────────┼─────────────┬───────────────┐
        ▼             ▼             ▼               ▼
   ┌─────────┐  ┌──────────┐  ┌─────────┐  ┌───────────┐
   │计算 Agent│  │网络 Agent │  │数据库   │  │安全 Agent  │
   │(ECS/ACK)│  │(VPC/SLB) │  │Agent    │  │(RAM/审计)  │
   └─────────┘  └──────────┘  └─────────┘  └───────────┘
                                              │
                                          ┌───────────┐
                                          │成本分析   │
                                          │Agent      │
                                          └───────────┘
```

**设计要点：**

- **主 Agent** 是用户统一对话入口，负责意图理解并调度 Sub-Agent
- **Sub-Agent** 各自维护独立上下文，可被主 Agent 委托独立执行专项任务
- Sub-Agent 之间通过**工具调用（Tool Call）**协作，不直接通信
- 用户可在主对话中通过 `@agent` 指令切换到特定 Sub-Agent 深入操作
- Dashboard 上展示各 Sub-Agent 的**最新状态**（如安全 Agent 扫描结果、成本 Agent 月度预估）

---

## 3. 信息架构（侧边栏设计）

### 3.1 设计原则

- 按**使用频率**分层：高频操作 → 侧边栏顶部，低频管理 → 底部
- 按**业务域**分组相关功能
- 侧边栏主入口控制在 **7-9 个**，避免过度碎片化

### 3.2 侧边栏结构

```
AliCloud Agent Hub
│
├── Dashboard              [高频] 总览首页
├── AI Chat                [高频] 主对话入口（主 Agent）
│
├── ─────────────── 资源管理 ───────────────
│
├── 资源总览                [中频] 云资源全局视图
│   ├── 计算 (ECS)
│   ├── 容器 (ACK / ASK)
│   ├── 数据库 (RDS / Redis)
│   └── 网络 (VPC / SLB / CDN)
│
├── 一键部署                [中频] 模板部署
│   ├── 常用模板
│   └── 部署历史
│
├── 运维工具                [中频] 批量运维
│   ├── 批量执行
│   └── 脚本中心
│
├── ─────────────── 辅助功能 ───────────────
│
├── 知识库                  [低频] 企业文档 + RAG
│
├── 企业配置                [低频] Agent + Skill 管理
│   ├── Agent Center
│   ├── Skill Center
│   └── 告警规则
│
└── 设置                   [低频] 系统设置
```

### 3.3 分类说明

| 分组 | 定位 | 包含内容 |
|------|------|---------|
| **高频核心** | 日常必用 | Dashboard、AI Chat |
| **资源管理** | 云资源操作 | 资源总览（ECS/ACK/RDS/VPC） |
| **一键部署** | 模板化部署 | 部署中心、部署历史 |
| **运维工具** | 批量运维 | 批量操作、脚本执行 |
| **辅助功能** | 低频配置 | 知识库、企业配置、设置 |

---

## 4. Dashboard 设计

### 4.1 页面布局

```
┌──────────────────────────────────────────────────────────┐
│ Header: AliCloud Agent Hub        [通知] [用户] [设置]  │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐        │
│  │ ECS     │ │ ACK     │ │ 本月成本 │ │ 活跃告警 │        │
│  │ 12 台   │ │ 3 集群   │ │ ¥12,450 │ │ 2 条    │        │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘        │
│                                                          │
│  ┌────────────────────────┐ ┌───────────────────────┐  │
│  │ Sub-Agent 状态面板      │ │ 快捷操作               │  │
│  │                         │ │                        │  │
│  │ • 安全扫描: 正常 ✓      │ │ [一键部署 ACK]          │  │
│  │ • 成本分析: 月均 ¥12k   │ │ [批量重启 ECS]          │  │
│  │ • 最近对话: 3 条未读    │ │ [安全扫描]              │  │
│  │                         │ │ [成本报告]              │  │
│  └────────────────────────┘ └───────────────────────┘  │
│                                                          │
│  ┌──────────────────────────────────────────────────┐   │
│  │ 成本趋势图（近30天）                              │   │
│  └──────────────────────────────────────────────────┘   │
│                                                          │
│  ┌──────────────────────────────────────────────────┐   │
│  │ 最近对话历史                                      │   │
│  └──────────────────────────────────────────────────┘   │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

### 4.2 Dashboard 模块说明

| 模块 | 内容 | 数据来源 |
|------|------|---------|
| **统计卡片** | ECS 数量、ACK 集群数、本月成本、活跃告警数 | 阿里云 API |
| **Sub-Agent 状态** | 各 Agent 最新运行状态、最近任务结果 | Agent Service |
| **快捷操作** | 一键部署 ACK、批量重启 ECS、安全扫描等 | 预设快捷操作 |
| **成本趋势图** | 近 30 天云资源费用走势图 | 阿里云 BSS API |
| **最近对话** | 最近 5 条对话记录快速入口 | Memory Service |

---

## 5. 核心功能模块设计

### 5.1 AI Chat（主对话）

**功能描述：** 用户与主 Agent 交互的统一入口。

**核心特性：**
- 自然语言输入，实时流式响应
- `@agent` 指令切换 Sub-Agent 上下文
- Skill / Knowledge Base 附加选择
- 实时执行日志展示（Execution Panel）
- 对话历史管理与上下文续接

**输入栏功能：**
- Knowledge Base 多选（文档、知识库）
- Skill 单选（可选，增强专项能力）
- 附件上传（待扩展）

---

### 5.2 对话模式切换（借鉴 Codex）

参考 Codex CLI 的ASK / Plan / Agent 三种模式，在对话页面顶部设计模式切换栏。

**三种模式：**

| 模式 | 图标 | 说明 | 适用场景 |
|------|------|------|---------|
| **Agent** | `fa-robot` | 全权限模式，可调用所有工具、读写文件、执行命令 | 写代码、部署资源、批量操作 |
| **Plan** | `fa-map` | 规划模式，先出计划，用户确认后再执行 | 复杂任务、部署流程、安全操作 |
| **Ask** | `fa-comment` | 只读模式，只回答问题，不做变更 | 查资料、问问题、看代码 |

**交互设计：**

```
┌──────────────────────────────────────────────────────────────┐
│  [🤖 Agent]  [🗺 Plan]  [💬 Ask]          当前模式：Agent  │
└──────────────────────────────────────────────────────────────┘
```

- 切换模式后，当前对话上下文保留，Agent 行为模式改变
- Plan 模式下：Agent 先输出 Plan（Markdown），用户点击「确认执行」后才真正执行
- Ask 模式下：Agent 只读不写，屏蔽所有写操作工具
- 快捷键：`Shift+Tab` 循环切换模式

**Plan 模式执行流程：**

```
用户输入 → Agent 理解意图 → 输出执行计划（Markdown）→ 用户确认/修改 → 真正执行
                                                              ↓
                                                    拒绝则放弃本次任务
```

**权限说明：**

| 模式 | 读文件 | 写文件 | 执行命令 | 调云 API | 调 Sub-Agent |
|------|--------|--------|---------|---------|-------------|
| Agent | ✅ | ✅ | ✅ | ✅ | ✅ |
| Plan | ✅ | ❌ | ❌ | ❌ | ❌ |
| Ask | ✅ | ❌ | ❌ | ❌ | ❌ |

---

### 5.3 代码展示面板（借鉴 Codex）

在对话页面右侧新增 **Code Panel（代码展示面板）**，实时展示 Agent 生成的代码文件。

**布局：**

```
┌──────────────────────────────────────────┬────────────────────────┐
│                                          │   Code Panel           │
│           AI Chat 主区域                   │                        │
│                                          │  📁 workspace/          │
│  用户：帮我写一个 Python 脚本              │   ├── main.py          │
│                                          │   ├── config.yaml      │
│  Agent：好的，正在生成...                  │   └── requirements.txt │
│                                          │                        │
│  [代码折叠预览区]                          │   [main.py]            │
│  ```python                               │   def main():          │
│  import requests                         │       ...              │
│  ...                                     │                        │
│  ```                                     │                        │
└──────────────────────────────────────────┴────────────────────────┘
```

**核心功能：**

| 功能 | 说明 |
|------|------|
| **文件树** | 展示 Agent 当前会话生成的所有文件 |
| **语法高亮** | 支持主流语言（Python、JS、YAML、Shell 等）|
| **折叠预览** | 代码流式生成时先展示前 10 行，用户可展开全文 |
| **文件操作** | 下载文件、复制代码、同步到 GitHub |
| **实时同步** | Agent 每生成/修改一个文件，右侧面板即时更新 |
| **文件对比** | 支持查看当前版本与历史版本的 diff |

**折叠预览交互（参考 Codex）：**

- 流式生成中：显示代码片段 + "..." 提示
- 生成完毕：显示完整代码，可展开/折叠
- 鼠标悬停：显示完整行数统计

---

### 5.4 命令行终端面板（借鉴 Codex）

在 Code Panel 下方或独立区域展示 Agent 执行命令的实时输出。

**功能设计：**

| 功能 | 说明 |
|------|------|
| **实时输出** | 命令执行时的 stdout / stderr 实时流式展示 |
| **分色显示** | stdout 白色、stderr 红色、info 蓝色 |
| **命令历史** | 展示本次会话执行过的所有命令 |
| **图标映射** | Shell 类命令显示 `fa-terminal` 图标 |
| **工具过滤** | Write / write_file 等写文件类工具不重复显示（已在 Code Panel 展示）|
| **执行时间** | 显示每个命令的执行耗时 |

**布局示意：**

```
┌─ 命令行终端 ──────────────────────────────────────┐
│  ▶ cd /workspace && pip install -r requirements.txt  │
│    Installing dependencies...                          │
│    ✅ Done in 3.2s                                    │
│                                                      │
│  ▶ python main.py                                     │
│    Server started on port 8080                        │
│    [INFO] Ready to accept connections                │
│                                                      │
│  ▶ git add . && git commit -m "feat: add script"     │
│    [main abc1234] feat: add script                   │
└─────────────────────────────────────────────────────┘
```

---

### 5.5 GitHub 同步（借鉴 Codex）

Agent 生成的代码可以一键同步到 GitHub 仓库。

**同步流程：**

```
用户指令（"推送到 GitHub"）
    ↓
Agent 生成代码 + Code Panel 更新
    ↓
用户点击 Code Panel 中的 [Sync to GitHub] 按钮
    ↓
展示分支选择 / Commit 信息填写
    ↓
执行 git add → git commit → git push
    ↓
返回同步结果（成功 / 失败）
```

**功能点：**

| 功能 | 说明 |
|------|------|
| **仓库配置** | 在设置中配置 GitHub Token + 仓库地址 |
| **分支管理** | 支持选择分支、新建分支 |
| **Commit 规范** | 自动生成 Commit Message，支持手动修改 |
| **冲突检测** | 推送前检测远程是否有冲突 |
| **自动同步** | 可配置任务完成后自动同步到指定分支 |
| **同步历史** | 记录每次同步的 Commit + 分支记录 |

**指令触发：**

```
用户：帮我写一个部署脚本，然后推送到 GitHub
    ↓
Agent 执行流程：
    1. 生成 deploy.sh
    2. Code Panel 展示文件
    3. 执行 git add → git commit → git push
    4. 返回 GitHub 链接
```

---

### 5.6 文档撰写与预览（借鉴 Codex）

支持生成 PDF / DOCX / PPT 等格式文档，并在 Agent 内置预览。

**支持格式：**

| 格式 | 说明 | 技术方案 |
|------|------|---------|
| PDF | 技术文档、报告 | reportlab / weasyprint |
| DOCX | 协作文档 | python-docx |
| PPT | 汇报材料 | python-pptx |
| Markdown | 轻量文档 | 直接渲染 |

**预览功能：**

- Agent 内置文档预览器，无需下载即可查看
- PDF 支持缩放、翻页、全屏
- PPT 支持幻灯片切换动画
- DOCX 转换为 HTML 预览

**文档生成流程：**

```
用户：帮我生成一份这个月的云资源成本报告，导出为 PDF
    ↓
Agent → 成本 Agent → 调用 BSS API 获取账单数据
    ↓
Agent 生成报告内容 → 调用 PDF 生成工具
    ↓
Code Panel 中显示生成的报告文件
    ↓
点击预览 → 内置 PDF 阅读器
    ↓
点击下载 → 保存到本地
```

**Code Panel 集成：**

```
📁 workspace/
  ├── 成本报告_2026-05.pdf      ← 新生成
  ├── 部署脚本.sh
  └── main.py
```

---

### 5.7 资源总览

**功能描述：** 统一查看和管理阿里云资源。

**子模块：**
| 模块 | 说明 |
|------|------|
| **计算 (ECS)** | ECS 实例列表、状态、操作（启动/停止/重启） |
| **容器 (ACK)** | ACK 集群管理、节点查看、工作负载 |
| **数据库 (RDS)** | RDS 实例列表、配置查看 |
| **网络 (VPC/SLB)** | VPC 网络拓扑、SLB 负载均衡配置 |

**设计要点：** 左侧按资源类型切换 Tab，右侧展示详情；支持通过 AI Chat 自然语言触发资源操作。

### 5.8 一键部署

**功能描述：** 模板化部署阿里云资源，降低运维门槛。

**模板类型：**
| 模板 | 说明 |
|------|------|
| **ACK 标准集群** | 3 节点标准集群，一键创建 |
| **ECS 批量扩容** | 批量创建指定规格 ECS |
| **LNMP 环境** | 一键部署 Linux + Nginx + MySQL + PHP |
| **自定义模板** | 用户可创建、保存自定义部署模板 |

**流程：** 选择模板 → 配置参数 → 确认部署 → 实时日志 → 部署完成

### 5.9 运维工具

**功能描述：** 批量运维操作，提升运维效率。

**功能点：**
- 批量选择 ECS 实例 → 批量重启/停止/删除
- 脚本执行（批量在多台机器执行同一命令）
- 预设运维脚本库

### 5.10 知识库

**功能描述：** 企业文档 + RAG 知识库，增强 Agent 回答准确性。

**功能点：**
- 上传企业文档（PDF、Markdown、TXT 等）
- 向量存储与语义检索
- 多知识库管理
- RAG 配置（召回策略、Top-K 参数）

### 5.11 企业配置

**功能描述：** 集中管理 Agent、Skill 及告警规则。

**子模块：**

| 模块 | 说明 |
|------|------|
| **Agent Center** | Sub-Agent 的配置、启用/禁用、参数调整 |
| **Skill Center** | Skill 的安装、卸载、参数配置 |
| **告警规则** | 配置资源告警阈值（CPU > 80%、内存不足等） |

### 5.12 Sub-Agent 设计（详细）

| Agent | 职责 | 触发方式 |
|-------|------|---------|
| **计算 Agent** | ECS/ACK 资源操作、扩缩容 | 主 Agent 调度 / `@compute` |
| **网络 Agent** | VPC/SLB/CDN 配置与操作 | 主 Agent 调度 / `@network` |
| **数据库 Agent** | RDS/Redis 操作与备份 | 主 Agent 调度 / `@database` |
| **安全 Agent** | RAM 扫描、合规检查、安全告警 | 主 Agent 调度 / `@security` |
| **成本 Agent** | 账单分析、优化建议、预算告警 | 主 Agent 调度 / `@cost` |

**注意：** Sub-Agent 不一定有独立对话页面，但在 Dashboard 上展示状态，主对话中通过指令调用。

---

## 6. 典型使用场景

### 场景 1：自然语言查资源

```
用户：帮我看看现在有几台 ECS，分别什么状态？
主 Agent → 计算 Agent
        → 调用阿里云 ECS API
        → 汇总返回：共 12 台 ECS，8 台运行中，4 台已停止
```

### 场景 2：一键部署 ACK

```
用户：帮我建一个 ACK 集群，3 个节点
主 Agent → 启动一键部署流程
        → 调用 ACK API 创建集群
        → 实时展示部署日志
        → 部署完成，返回集群信息
```

### 场景 3：成本分析与优化

```
用户：这个月云资源花了多少钱？
主 Agent → 调度成本 Agent
        → 调用 BSS API 获取账单
        → 生成成本报告 + 优化建议
```

### 场景 4：批量运维

```
用户：批量重启这 10 台 ECS
主 Agent → 调用 ECS API 批量操作
        → 实时展示执行进度
        → 返回操作结果汇总
```

### 场景 5：安全审计

```
用户：扫描一下 RAM 账号权限配置
主 Agent → 调度安全 Agent
        → 调用 RAM API 检查权限
        → 生成安全报告：高危/中危/低危
```

---

## 7. 技术栈（当前）

| 层级 | 技术 |
|------|------|
| **前端** | HTML + CSS + Vanilla JS（当前）|
| **后端** | Python Flask |
| **AI 模型** | 阿里云 DashScope（Qwen3 Max/Plus）|
| **知识库** | 向量检索（RAG）|
| **云 API** | 阿里云 OpenAPI |

---

## 9. 待讨论 / 后续事项

- [ ] 确认前端框架是否迁移到 React/Vue（当前为 Vanilla JS）
- [ ] Sub-Agent 的技术实现方案（独立服务还是同一进程内的模块）
- [ ] 阿里云 API Key 的安全存储方案
- [ ] 多租户 / 权限控制设计
- [ ] 对话历史的存储与导出方案
- [ ] 前端 UI 改版实现（对话模式切换 + Code Panel + Terminal Panel）
- [ ] PDF / DOCX / PPT 生成与预览的技术选型
- [ ] GitHub 集成的 Token 安全存储方案
- [ ] Plan 模式的"确认执行"前端交互设计
- [ ] 命令行 Terminal Panel 的流式输出方案（SSE 事件类型定义）

---

## 10. 参考与灵感来源

以下产品/工具的设计理念被部分参考：

| 来源 | 参考内容 |
|------|---------|
| **Codex CLI** | ASK/Plan/Agent 三模式切换、代码折叠预览、命令执行日志、文件树 |
| **Codex Cloud** | 任务提交后端执行、前端实时获取结果 |
| **Cursor IDE** | AI 对话界面、工具调用可视化、Approval 权限控制 |
| **阿里云控制台** | 资源管理界面、云产品导航结构 |

---

## 11. 修订历史

| 版本 | 日期 | 修改内容 |
|------|------|---------|
| v1.0 | 2026-05-17 | 初始版本，整合所有讨论内容 |
| v1.1 | 2026-05-17 | 新增对话模式切换（5.2）、代码展示面板（5.3）、命令行终端（5.4）、GitHub 同步（5.5）、文档撰写预览（5.6），参考 Codex 功能设计 |
| v1.2 | 2026-05-17 | Phase 1 完成：合并两个聊天 UI，清理侧边栏导航；Phase 2 完成：Agent/Plan/Ask 模式切换后端 + 前端实现，包含 Plan 确认流程 |
