# Engineer Agent 前端设计文档

## 1. 设计风格概述

### 1.1 设计理念
本项目采用**简约、专业、企业级**的设计风格，参考Manulife（宏利）的网站设计语言，强调清晰、专业、易用。

### 1.2 核心特点
- **直角设计**：所有组件使用直角边框，无圆角或小圆角
- **绿色主色调**：以Manulife绿为主色，体现专业与信任
- **简洁布局**：减少视觉噪音，突出内容本身
- **功能清晰**：每个元素都有明确的用途和状态

---

## 2. 配色系统

### 2.1 主色调

#### 主色 (Primary) - Manulife Green
```css
Primary: #00693C (RGB: 0, 105, 60)
```
**用途**：主要按钮、激活状态、强调元素、图标

#### 次色 (Secondary) - Deep Blue
```css
Secondary: #002D62 (RGB: 0, 45, 98)
```
**用途**：次要按钮、辅助强调、数据可视化

### 2.2 中性色

#### 浅灰色 (Light Grey)
```css
Manulife Light Grey: #E8E8E8
```
**用途**：边框、分割线、禁用状态背景

#### 浅灰背景 (Light Grey Background)
```css
Manulife Light Grey BG: #F5F5F5
```
**用途**：悬停状态背景、卡片背景、次要背景

#### 灰色 (Grey)
```css
Manulife Grey: #6B7280
```
**用途**：次要文本、标签文字、图标颜色

### 2.3 状态色

#### 成功色 (Success)
```css
Success Border: #10B981
Success Background: #ECFDF5
Success Text: #059669
```

#### 警告色 (Warning)
```css
Warning Border: #F59E0B
Warning Background: #FFFBEB
Warning Text: #D97706
```

#### 错误色 (Error)
```css
Error Border: #EF4444
Error Background: #FEF2F2
Error Text: #DC2626
```

#### 信息色 (Info)
```css
Info Border: #3B82F6
Info Background: #EFF6FF
Info Text: #2563EB
```

### 2.4 文字颜色

```css
Text Primary: #111827 (标题、主要文字)
Text Secondary: #374151 (次要文字)
Text Muted: #6B7280 (辅助文字、标签)
Text Inverse: #FFFFFF (在深色背景上的文字)
```

---

## 3. 组件设计

### 3.1 按钮 (Button)

#### 默认按钮
- 背景：`#00693C` (Primary)
- 文字：白色
- 悬停：Primary/90%
- 激活：Primary/80%
- 边框：直角，无圆角

#### 次要按钮
- 背景：`#002D62` (Secondary)
- 文字：白色
- 悬停：Secondary/80%

#### 轮廓按钮
- 背景：透明
- 边框：`#E8E8E8`
- 文字：`#374151`
- 悬停背景：`#F5F5F5`

#### 幽灵按钮
- 背景：透明
- 文字：`#6B7280`
- 悬停背景：`#F5F5F5`

#### 按钮尺寸
```css
Default: h-10 px-4 py-2 (40px高度)
Small: h-9 px-3 (36px高度)
Large: h-11 px-8 (44px高度)
Icon: h-10 w-10 (40x40px)
```

### 3.2 输入框 (Input)

```css
边框颜色: #E8E8E8
聚焦边框: #00693C (Primary)
聚焦环: 1px solid Primary
背景: 白色
高度: 40px
内边距: 0 12px
圆角: 0 (直角)
```

### 3.3 卡片 (Card)

```css
背景: 白色
边框: 1px solid #E8E8E8
内边距: 20px
圆角: 0 (直角)
悬停: 可选阴影或边框变为主题色
```

### 3.4 对话框 (Dialog)

```css
遮罩背景: rgba(0, 0, 0, 0.5)
内容背景: 白色
边框: 无
内边距: 24px
圆角: 0 (直角)
最大宽度: sm:540px, md:600px, lg:800px, xl:900px
```

### 3.5 标签 (Badge)

```css
背景: #F5F5F5
边框: 1px solid #E8E8E8
文字: #6B7280
圆角: 0 (直角)
内边距: 4px 8px
字号: 12px (xs)
```

### 3.6 开关 (Switch)

```css
关闭状态轨道: #E8E8E8
开启状态轨道: #00693C
滑块: 白色
圆角: 小圆角或直角
```

### 3.7 下拉菜单 (Dropdown)

```css
背景: 白色
边框: 1px solid #E8E8E8
阴影: 小阴影
圆角: 0 (直角)
项悬停背景: #F5F5F5
```

---

## 4. 布局设计

### 4.1 整体布局

```
+------------------+----------------------------------------+
|                  |              Header (56px)              |
|    Sidebar       +----------------------------------------+
|   (200-400px)    |                                        |
|                  |              Main Content               |
|  可拖拽调整宽度   |                                        |
|                  |                                        |
|                  +----------------------------------------+
|                  |           Right Panel (可选, 320px)     |
+------------------+----------------------------------------+
```

### 4.2 侧边栏 (Sidebar)

#### 结构
- 顶部：Logo区域 + 新建对话按钮
- 中部：功能菜单（分组显示）
- 底部：历史记录 + 用户信息

#### 宽度
- 最小宽度：200px
- 默认宽度：260px
- 最大宽度：400px
- 可拖拽调整

#### 菜单项
- 高度：40px
- 内边距：12px 水平
- 图标：20x20px
- 激活状态：左边框2px Primary + 渐变背景

#### 颜色区分
- 分类标题前：绿色小圆点标识
- 激活项：绿色文字 + 绿色左边框 + 浅绿渐变背景
- 用户区域：绿色渐变头像 + 在线状态指示

### 4.3 头部 (Header)

#### 高度
```css
高度: 56px (h-14)
内边距: 0 24px
边框底部: 1px solid #E8E8E8
背景: 白色
```

#### 内容
- 左侧：Logo + 产品名称
- 右侧：设置按钮 + 通知按钮 + 用户头像

### 4.4 主内容区

#### 对话页面布局
```
+----------------------------------+------------------+
|                                  |   Agent执行面板   |
|        对话区域                   |    (320px)       |
|                                  |                  |
|   - 消息列表                      |  - 任务分解       |
|   - 空状态欢迎页                   |  - 实时日志      |
|                                  |                  |
+----------------------------------+------------------+
|        输入区域                    |                  |
|   - 知识库选择下拉                 |                  |
|   - Skill选择下拉                 |                  |
|   - 文本输入框                    |                  |
|   - 发送按钮                      |                  |
+----------------------------------+------------------+
```

---

## 5. 字体系统

### 5.1 字体家族
```css
主要字体: Inter, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif
代码字体: "JetBrains Mono", Monaco, Consolas, monospace
```

### 5.2 字号系统
```css
xs: 12px   (标签、次要信息)
sm: 14px   (正文、辅助文字)
base: 16px (主要文字)
lg: 18px   (标题、小标题)
xl: 20px   (页面标题)
2xl: 24px  (大标题)
3xl: 30px  (主标题)
```

### 5.3 字重
```css
normal: 400   (正文)
medium: 500   (次要标题)
semibold: 600 (主要标题)
bold: 700     (强调)
```

---

## 6. 间距系统

### 6.1 基础间距单位
```css
1单位 = 4px
2单位 = 8px
3单位 = 12px
4单位 = 16px
5单位 = 20px
6单位 = 24px
8单位 = 32px
```

### 6.2 常用间距
```css
元素内间距: 12px-16px
卡片内边距: 20px
区块间距: 24px
页面边距: 24px-32px
```

---

## 7. 阴影系统

### 7.1 阴影级别
```css
shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.05)
shadow-md: 0 4px 6px rgba(0, 0, 0, 0.1)
shadow-lg: 0 10px 15px rgba(0, 0, 0, 0.1)
```

### 7.2 使用场景
- 卡片悬停：可选shadow-md
- 对话框：shadow-lg
- 下拉菜单：shadow-md

---

## 8. 动效设计

### 8.1 过渡时长
```css
过渡时长: 150ms-200ms
缓动: ease-in-out
```

### 8.2 交互反馈
- 按钮悬停：背景色变化
- 激活状态：左边框出现 + 渐变背景
- 下拉展开：平滑展开动画
- 拖拽：实时更新宽度 + 边缘高亮

### 8.3 状态动画
- 在线状态：呼吸动画（pulse）
- 加载状态：旋转动画
- 成功/错误：短暂高亮后恢复

---

## 9. 响应式设计

### 9.1 断点
```css
sm: 640px   (小型平板)
md: 768px   (平板)
lg: 1024px  (桌面)
xl: 1280px  (大桌面)
2xl: 1536px (超大桌面)
```

### 9.2 网格系统
```css
grid-cols-1   (手机)
grid-cols-2   (平板)
grid-cols-3   (桌面)
```

---

## 10. 图标系统

### 10.1 图标库
使用 **Lucide React** 图标库

### 10.2 图标尺寸
```css
icon-sm: 16px (16x16)
icon-default: 18px (18x18)
icon-lg: 20px (20x20)
icon-xl: 24px (24x24)
```

### 10.3 图标颜色
```css
默认: #6B7280 (Manulife Grey)
激活: #00693C (Primary Green)
悬停: #111827 (深色)
```

---

## 11. 表单元素

### 11.1 输入框状态
- 默认：灰色边框
- 聚焦：绿色边框 + 绿色光环
- 错误：红色边框 + 错误提示
- 禁用：灰色背景 + 浅色文字

### 11.2 复选框
```css
未选中边框: #E8E8E8
选中背景: #00693C
选中图标: 白色勾选
尺寸: 16x16px
```

### 11.3 开关
```css
关闭: 灰色轨道
开启: 绿色轨道 + 白色滑块
尺寸: 44x24px
```

---

## 12. 列表设计

### 12.1 列表项
```css
高度: 48px-64px
边框: 底部1px #E8E8E8
悬停背景: #F5F5F5
内边距: 12px 16px
```

### 12.2 列表分组
```css
分组标题: 12px, #6B7280, 大写, 左边距
分组内容: 缩进或左边框区分
```

---

## 13. 空状态设计

### 13.1 空状态组件
```css
图标容器: 64x64px, 灰色背景
图标: 32px, 灰色
标题: 18px, 中等字重
副标题: 14px, 灰色
操作按钮: Primary绿色
```

---

## 14. 状态显示

### 14.1 加载状态
- 骨架屏：灰色占位
- 旋转指示器：Primary绿色
- 进度条：Primary绿色填充

### 14.2 进度指示
```css
步骤完成: 绿色圆形 + 白色勾选
步骤进行中: 橙色 + 脉冲动画
步骤待处理: 灰色圆形
步骤错误: 红色圆形
```

---

## 15. 日志显示

### 15.1 日志面板
```css
背景: 白色
边框: 1px solid #E8E8E8
内边距: 16px
字体: 等宽字体, 12px
行高: 1.5
```

### 15.2 日志条目
```css
信息: 左侧蓝色边框
成功: 左侧绿色边框
警告: 左侧橙色边框
错误: 左侧红色边框
时间戳: 灰色等宽字体
```

---

## 16. 代码规范

### 16.1 颜色使用规范
```typescript
// Tailwind配置中使用自定义颜色
primary: "#00693C"           // 主色
secondary: "#002D62"         // 次色
manulife-green: "#00693C"    // 绿色
manulife-lightGrey: "#E8E8E8" // 浅灰
manulife-lightGreyBg: "#F5F5F5" // 浅灰背景
manulife-grey: "#6B7280"      // 灰色
```

### 16.2 圆角规范
```typescript
// 所有圆角设为0，保持直角设计
borderRadius: {
  lg: "0",
  md: "0",
  sm: "0",
}
```

---

## 17. 设计资源

### 17.1 颜色参考
- **Manulife绿**: #00693C (RGB: 0, 105, 60)
- **Manulife蓝**: #002D62 (RGB: 0, 45, 98)
- **辅助灰**: #6B7280
- **边框灰**: #E8E8E8
- **背景灰**: #F5F5F5

### 17.2 设计工具
- Figma (设计稿)
- Tailwind CSS (样式实现)
- shadcn/ui (组件库)
- Lucide React (图标库)

---

## 18. 实现指南

### 18.1 新增颜色
在 `tailwind.config.ts` 的 `colors` 中添加：
```typescript
manulife: {
  green: "#00693C",
  blue: "#002D62",
  lightGrey: "#E8E8E8",
  lightGreyBg: "#F5F5F5",
  grey: "#6B7280",
}
```

### 18.2 新增组件
1. 在 `src/components/ui/` 中创建组件
2. 使用 `cn()` 工具函数合并类名
3. 遵循直角设计原则
4. 使用Manulife配色系统

### 18.3 页面开发
1. 使用 `max-w-none` 移除内容宽度限制
2. 保持16px基础内边距
3. 使用分组和分割线组织内容
4. 确保响应式适配

---

## 19. 更新日志

### 2024-xx-xx - 初始版本
- 建立配色系统
- 定义组件样式
- 制定布局规范
- 完成设计文档
